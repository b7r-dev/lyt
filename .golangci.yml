# golangci-lint configuration for lyt

version: "2"

run:
  timeout: 5m
  modules-download-mode: readonly
  tests: false

linters:
  enable:
    - errcheck
    - govet
  disable:
    - staticcheck

issues:
  exclude-use-default: false
